// Wuanberri — Stripe config stub
// Fill in your Stripe publishable key (pk_test_... in dev, pk_live_... in prod).
// Empty by default — the "Start trial" button stays a no-op until this is set.
//
// To enable:
//   1. Stripe dashboard → Developers → API keys → copy the Publishable key
//   2. Paste it below (between the quotes, NO trailing whitespace)
//   3. Commit. Vercel will deploy. The Stripe.js script will lazy-load on first click.
window.STRIPE_CONFIG = {
  publishableKey: '',  // e.g. 'pk_test_51Nxxxxxxxxxxxxxxxxxxxxxxxx'
};
